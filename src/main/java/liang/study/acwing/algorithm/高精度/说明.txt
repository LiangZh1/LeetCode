+ - A+B  A-B 数的长度 <=10^6
*  A*a 一个大数的长度 len(A) <= 10^6  a<=10000

把大整数的每一位存在数组中

87654321

0 1 2 3 4 5 6 7 8 (数组位置)
8 7

0位存个位，高位存在数组的高位，因为可能要进位

不管A和B是正数还是负数，绝对可以转换成绝对值的加或者减

高精度加法

高精度减法

A1  A2  A3
    B1  B2

t = 0
t = Ai - t
t = t - Bi
    t >= 0  t+10
    t<0     t
            统一抽象为 (t+10) % 10
if t < 10
    t = 1
else
    t = 0



高精度乘法
A1  A2  A3
        B
C0 = (A3 * B) % 10
T1 = (A3 * B) / 10
C1 = (A3 * B + T1) % 10

高精度除法
